import { FlatList, Image, Text, View } from "react-native";

const ImagesList = ({ images }) => {
  // currently not working
  // return (
  //   <FlatList
  //     horizontal={true}
  //     data={images}
  //     renderItem={(image) => {
  //       return <Text>{image.uri}</Text>;
  //     }}
  //   />
  // );
};

export default ImagesList;
